import React from "react";

export default function CompareTour({setCurrentStep}: {setCurrentStep: React.SetStateAction<number>}) {
    return (
        <div className="w-screen h-screen flex flex-col items-center justify-between">

        </div>
    )
}