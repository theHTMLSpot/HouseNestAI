import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

};
module.exports = {
  images: {
    domains: ['example.com'], // Add 'example.com' to the list of allowed domains
  },
};


export default nextConfig;
